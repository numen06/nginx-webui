version: '3.8'

services:
  nginx-webui:
    image: registry.cn-shanghai.aliyuncs.com/numen/nginx-webui
    ports:
      - "8001:8001"
      - "443:443"
      - "80:80"
    volumes:
      - /opt/nginx-webui/data:/app/data
    environment:
      - APP_PORT=8001
    restart: always


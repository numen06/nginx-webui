version: '3.8'

services:
  nginx-webui:
    image: registry.cn-shanghai.aliyuncs.com/numen/nginx-webui
    ports:
      - "8001:8001"
    volumes:
      - /opt/nginx-webui/nginx:/app/nginx
      - /opt/nginx-webui/data:/app/data
    environment:
      - APP_PORT=8001
    restart: unless-stopped

